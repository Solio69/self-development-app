{
  "name": "self-development-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "server:backend": "yarn workspace self-development-app-backend server",
    "start:frontend": "yarn workspace self-development-app-frontend start",
    "dev": "concurrently \"yarn server:backend\" \"yarn start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.7",
    "colors": "^1.4.0"
  }
}
