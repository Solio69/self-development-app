{
  "name": "self-development-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "yarn workspace self-development-app-backend start",
    "start:backend": "yarn workspace self-development-app-backend start",
    "server:backend": "yarn workspace self-development-app-backend server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.7",
    "colors": "^1.4.0"
  }
}
